# Commands 

- `deno task test` - run tests 
- `deno task run-coverage` - run *after* run tests. gen coverage report.
- `deno task format` - format code 
- `deno task lint` - lint code
- `deno task compile` - make binary for current platform. good way do type-check codebase beyond linter and tests.